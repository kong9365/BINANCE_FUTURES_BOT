# 전략 현실성 분석 & 개선 계획 — Binance USDT-M 선물 봇

> 작성: 2026-05-21 | 분석 데이터: 캐시 12종목 × 501 bar(1h) = 6,012 bar(~20일) | 실거래 GO: **HOLD 유지**

## Context (왜 이 문서가 존재하는가)
실거래 투입 전 안정화 과정에서, 현 매수 전략(OI-급증 모멘텀)이 (a) 현 임계로 사실상 무거래,
(b) 거래해도 끝난 스파이크를 추종(역선택)한다는 의심이 제기됐다. 본 문서는 이를 **실데이터로
정량 검정**하고, 그 결과에 따라 **검증 가능한 엣지를 가진 전략으로 교체**하는 단계별 계획을
제시한다. 또한 운영자 요청으로 모든 영속 데이터를 **Supabase(주저장) + 로컬 sqlite(안전망)**로
이관한다. 각 단계는 **검증 게이트**를 통과해야 다음으로 넘어가며, 회귀를 방지한다.

---

## 1. 정량 진단 결과 (현 OI-급증 전략)

### 1-A. 재보정 그리드 — 64개 전 조합이 손실
(룩백 L × OI임계 × 가격임계) 격자 전수 백테스트(TP+2%/SL−1%, 최대보유 24h, 왕복비용 0.26% 반영):

| 구성 | 거래 | 승률 | 기대값/트레이드 | PF |
|---|---|---|---|---|
| **최선** L=1, OI≥1.5%, P≥0.7% | 26 | 38.5% | **−0.064%** | 0.91 |
| L=3, OI≥1.0%, P≥0.7% | 93 | 35.5% | −0.241% | 0.69 |
| L=6, OI≥1.0%, P≥0.5% | 129 | 32.6% | −0.276% | 0.65 |
| 대부분의 조합 | — | 15~35% | −0.3 ~ −0.9% | 0.2~0.7 |

→ **"가장 덜 나쁜" 조합조차 PF 0.91로 손실.** 임계 재보정으로는 흑자 전환 불가.

### 1-B. 역선택 검정 — 엣지의 *부호*를 직접 측정 (핵심)
신호 발생 후 *신호 방향*으로의 평균 forward return:

| 보유 | OI≥1.5%/P≥0.7% (신호 38건) | OI≥1.0%/P≥0.5% (신호 80건) |
|---|---|---|
| +1 bar | +0.176% (양 63%) | −0.044% (양 51%) |
| +3 bar | +0.151% (양 55%) | −0.072% (양 49%) |
| +6 bar | **−0.029% (양 47%)** | **−0.270% (양 46%)** |
| +12 bar | +0.100% (양 55%) | −0.213% (양 46%) |

→ 진짜 모멘텀은 **1~3시간만 약하게(+0.15%)** 살아있다가 **6시간 내 소멸·반전**. 그 +0.15%가
**왕복 비용 0.26%보다 작다.** 신호를 느슨하게 풀면(거래↑) **전 구간 음수**(순수 평균회귀).

### 1-C. 결론
- 현 전략은 **재보정 대상이 아니라 교체 대상**. 20일 표본의 모든 파라미터에서 음의 기대값.
- 미세한 단기 모멘텀 < 비용. **TP2/SL1 구조의 1% 손절이 알트 노이즈 안**이라 승률 15~38%로 깔림.
- ⚠️ 한계: 20일·단일 레짐·26~130거래 → **방향성은 강하나 통계적 확정은 아님**. 그래서 교체 후
  **다년 walk-forward 검증(거래≥200)**이 필수(P4).

---

## 2. 전략 리서치 & 채택

7개 전략군(펀딩 캐리/극단, OI+CVD, 돌파, 추세추종, 평균회귀, 횡단면 모멘텀, 스탯아브)을 조사.
제약(스팟 레그 금지=보호자산 룰, 틱/L2 데이터 없음, 비용 0.26% → 0.5%+ 움직임 필요, 보수적·백테스트
가능·~일단위 빈도)에 비추어 **레짐 전환형 단일 봇**을 채택.

| 모드 | 전략 | 엣지 가설 | 프로파일 |
|---|---|---|---|
| **추세장(ADX>25)** | **Donchian/ATR 돌파** | 암호화폐의 강한 추세 지속성 — N기간 고점 돌파 = 새 추세, 두꺼운 우측꼬리 포착 | 저승률(35~45%)·고R(2.5~4:1) |
| **횡보장(ADX<20)** | **펀딩 극단 역추세 페이드 (OI 확인)** | 극단 펀딩 = 한쪽 쏠림(군중). 한계수요 소진 시 프리미엄 붕괴·청산 → **소진된 쪽을 페이드**. 현 전략 역선택의 거울상 | 고승률(45~60%)·R 1.5~2.5:1 |

**채택 근거**: 둘 다 보유 데이터(OHLCV+펀딩+OI)로 백테스트 가능, 스팟 레그 불필요(보호자산 룰 무충돌),
~일단위·보수적, 목표 움직임 ≥0.5~1.5%로 비용 상회, 추세 vs 회귀라 **음의 상관**으로 자본곡선 평탄화.
**v2 보류**: 횡단면 모멘텀(바스켓 실행·유니버스 관리 복잡). **제외**: 캐리/스탯아브(스팟·MM 인프라 필요).

**구현 순서(운영자 결정)**: ① **돌파 먼저**(OHLCV만, 가장 단순·견고·즉시 백테스트) → ② 펀딩 페이드 나중.

> 출처: ScienceDirect 2025(펀딩 아브 위험/수익), CryptoCred(OI/CVD), Turtle/Donchian(추세),
> Han·Kang·Ryu SSRN 4675565(암호 모멘텀 Sharpe≈1.5), Stoic Research(ORB Sharpe≈1) 등.
> 상세 리서치는 본 세션 리서치 산출물 참조.

---

## 3. 데이터 영속화 (Supabase 주저장 + 로컬 안전망)

운영자 요청: 모든 DB(거래·체결·시그널·자본·로그)를 Supabase에 저장.
**채택 방식**: Supabase(Postgres 17, 프로젝트 `aoeqgptytzuhvvchsgul` "Binance") **주저장** +
로컬 sqlite **폴백 큐**(네트워크 장애 시 거래 중단 방지, 재연결 시 동기화). 트레이딩 루프 안정성 보장.

**런타임 접근**: 봇은 `supabase-py`(또는 psycopg)로 접속(env: `SUPABASE_URL`/`SUPABASE_KEY`,
시크릿은 .env·미커밋). 스키마 마이그레이션·검증 쿼리는 Claude의 Supabase MCP로 관리.

**스키마(초안, public)**:
| 테이블 | 용도 |
|---|---|
| `trades` | 거래 생애주기(진입·청산·PnL·R·수수료·레짐·전략·사유) |
| `signals` | 신호/게이트 통과 여부 + 피처(jsonb) |
| `position_events` | 포지션 상태 변화 이벤트 |
| `equity_snapshots` | 자본 스냅샷(wallet/available/margin/unrealized) |
| `oi_history` / `funding_history` | OI·펀딩 롤링 누적(바이낸스 30일 한계 극복) |
| `ohlcv` | 캔들 누적(taker_buy_base 포함 — CVD 프록시용) |
| `regime_log` | 레짐 분류 이력(ADX/EMA/ATR) |
| `events` | 앱 로그·에러(레벨·컴포넌트·메시지) |
| `backtest_runs` | 백테스트 실행 결과(파라미터 jsonb·지표) — 회귀 기준선 |

시간은 모두 `timestamptz`(UTC), 유연 필드는 `jsonb`.

---

## 4. 단계별 개선 계획 (각 단계 = 검증 게이트 통과 필수)

> 공통 회귀 방지: 매 단계 ⓐ `pytest tests/` 전체 그린, ⓐ 신규 모듈 `tests/test_*.py` 동반,
> ⓒ 백테스트 룩어헤드 가드(close 확정 후 신호, `shift(-1)` 금지), ⓓ 기준 지표를 `backtest_runs`에
> 기록해 재실행 시 회귀 감지, ⓔ "A안" GitHub 반영(보안스캔→테스트→명시 add→커밋→push).

### P0 — Supabase 영속 계층 (주저장 + 로컬 폴백)
- Supabase 스키마 적용(MCP migration). 봇 `data/persistence.py` 어댑터: Supabase 쓰기 + 실패 시
  로컬 sqlite 큐 적재 → 재연결 시 flush. 기존 sqlite 스키마와 호환 매핑.
- **검증**: mock 라운드트립 단위테스트, 네트워크 장애 폴백·재동기 테스트, `list_tables`로 스키마 확인,
  실제 1행 write→read 통합 점검(read-only 데이터).

### P1 — 레짐 엔진 + Donchian/ATR 돌파 전략 (백테스트)
- `strategy/breakout.py`(또는 기존 strategy 확장): ADX(14)+200EMA 레짐, Donchian(20/55) 돌파,
  ATR(14) 기반 초기 스톱(~2 ATR)·사이징, 청산=반대 채널(10기간)/ATR 트레일.
- `backtesting/backtest_engine.py`에 `strategy="breakout"` 추가(룩어헤드 차단).
- **검증**: 지표 정확성 단위테스트(알려진 값), 룩어헤드 테스트, 캐시+누적 데이터 백테스트로
  기대값·PF·MDD 측정 후 `backtest_runs` 기록. (저승률·고R 특성 확인.)

### P2 — 라이브 통합 (HOLD 유지)
- 돌파 신호를 라이브 결정 경로에 통합(RiskManager/CostGuard/PairWhitelist/보호종목/preflight **그대로**).
- **검증**: 통합 테스트, testnet dry-run(exit 0·ERROR 0), preflight·예산 cap 동작. **실거래 GO=HOLD.**

### P3 — 펀딩 극단 페이드 (횡보 모드)
- 펀딩/OI 누적 선행 → percentile 극단 + OI 롤오버 확인 역추세 페이드(ADX<20 게이트).
- **검증**: 백테스트(횡보 구간), 단위테스트, knife-catch 방지(확인 트리거) 검증.

### P4 — 데이터 누적 + walk-forward 튜닝
- OHLCV/펀딩/OI 누적 확장(P0 테이블에 적재) → 수개월 후 walk-forward(거래≥200, OOS Sharpe≥1.0,
  PF≥1.2, MDD<20%) 검증·튜닝. 통과 못하면 전략 재검토/폐기.
- **검증**: walk-forward 지표, OOS 성능, 기준선 대비 회귀 체크.

### P5 — 시장-컨텍스트 피처 & risk-off 게이트 (운영자 제안 §6 반영)
- **P5a 데이터(=P4 누적 확장, 즉시 가치)**: 비트코인을 시장-베타 인덱스로 **항상** 적재 —
  `BTCUSDT`(+`ETHUSDT`) OHLCV 를 수집 유니버스에 추가(거래 아님, **read-only 인덱스 전용**;
  보호종목 거래 금지 룰 불변). CMC 글로벌 지표(BTC 도미넌스, 총 시총, 선택적 Fear&Greed)를
  신규 테이블 `market_global` 에 시간별 적재(CMC 레이트리밋·지연 고려, 1콜/주기).
- **P5b 전략 게이트(백테스트 선행, 라이브는 HOLD)**: BTC 추세 **risk-off 게이트** — BTC 가 확정
  하락(예: 200EMA 하단 + ADX 추세)일 때 알트 **신규 LONG 차단**(상관 1 수렴 시 동반 급락 회피).
  도미넌스 급등(자금 BTC 회귀)도 보조 risk-off 신호로 검토. **단순 추가 금지** — 게이트가
  expectancy↑·MDD↓ 를 실제로 개선하는지 백테스트로 입증된 경우에만 채택.
- **검증**: BTC 인덱스/CMC 적재 round-trip(MCP), 게이트 on/off 백테스트 비교(거래수·승률·
  expectancy_R·MDD, `backtest_runs` 기록), 룩어헤드 차단(BTC 마감봉만), 라이브 통합 시 dry-run.

---

## 5. 진행 상태
- ✅ 정량 진단(§1) 완료 — 현 전략 음의 기대값·역선택 확정.
- ✅ 전략 리서치·채택(§2) 완료 — 돌파 먼저 → 펀딩 페이드.
- ✅ **P0 완료** — Supabase 19테이블(RLS) + `data/persistence.py`(주저장+sqlite 폴백 큐),
  단위테스트 8 + 실연결 라운드트립. 전체 340 passed. (커밋 `26e50cc`)
- ✅ **P1 완료** — `strategy/breakout.py`(EMA/ATR/Wilder-ADX/Donchian 자체구현, 룩어헤드
  안전, 라이브/백테스트 공유) + `BacktestEngine strategy="breakout"`. 단위테스트 9, 전체
  349 passed. 캐시 20일 백테스트: 기본/완화 구성 승률 ~46–47%·expectancy_R +0.11·PF
  0.82–1.06(저승률·고R 추세추종 프로파일) — OI-급증(전 조합 음수)의 질적 반전. **단
  13–19거래/단일레짐 = 통계적 검증 아님**(P4 필요). 기준선 `backtest_runs`
  (`p1_breakout_baseline_20260521`)에 기록(회귀 가드). (커밋 `d673e13`)
- ✅ **P2 완료** — 라이브 활성 전략 선택자(`StrategyConfig.active_strategy`, env `ACTIVE_STRATEGY`,
  기본 oi_surge로 기존 동작 불변) + `main_7590._scan_breakout`(형성중 캔들 제외=룩어헤드 차단,
  `collector.get_candles`로 실시간 평가). 게이트 ④~⑦(리스크→사이징→CostGuard→실행)을 공유
  `_execute_decision`로 추출해 oi_surge/breakout가 **동일 게이트·보호종목·예산cap** 통과(괴리
  방지). 통합테스트 5 + 기존 39 무회귀, 전체 354 passed. dry-run 스모크(ACTIVE_STRATEGY=breakout)
  exit 0·에러 0·정상종료. **실거래 GO HOLD 유지.** (커밋 예정)
- 🔄 **P4 진행 중** — **데이터 누적 확장 완료**: `collect_oi` 가 OHLCV/OI/펀딩을 Supabase
  (`ohlcv`/`oi_history`/`funding_history`)에 멱등(upsert) 적재(persistence 배치 `upsert_many`,
  스케줄 실행은 최근 48봉, `--backfill` 전체 시딩). 백필 검증: ohlcv 6,036 / oi 6,036 /
  funding 2,400행(12종목, 2026-04-30~05-21) Supabase 적재 확인. 단위테스트 +7, 전체 361 passed.
  **남은 것: 수개월 누적 후 walk-forward(거래≥200, OOS)** — 시간 경과 대기(스케줄 자동 누적).
- ✅ **P5a 완료**(운영자 제안 §6) — BTC/ETH **인덱스 OHLCV 상시 적재**(거래 아님, read-only) +
  CMC `get_global_metrics`(도미넌스·총시총·F&G) → 신규 `market_global` 테이블. 백필 검증: 인덱스
  ohlcv 1,000행(BTC+ETH), market_global 1행(BTC dom 60.1%·총시총 $2.57조·F&G 39). 단위테스트
  +4, 전체 365 passed. 스케줄 수집이 매시간 인덱스·글로벌도 자동 누적.
- ✅ **#2 돌파 청산 정교화 완료(구현, 기본 OFF)** — `BacktestConfig.breakout_trail_exit`(ATR 샹들리에
  트레일, opt-in). **검증 결과 21일 표본에선 트레일이 더 나쁨**(승률 41%→23%, expR +0.04→−0.26):
  지속 추세 없는 단기 구간에서 휩쏘. **기본 고정 TP4/SL2 유지**, 트레일 채택은 P4 walk-forward에서
  결정. 기준선 `backtest_runs`(`breakout_trail_vs_fixed_20260521`) 기록. 단위테스트 +2, 전체 367 passed.
- ✅ **과거 backfill 완료** — `backtesting/backfill_history.py`(페이지네이션 klines/funding +
  taker_buy_base). Supabase에 **2년치 245,280 ohlcv(14종목·730일·taker 100%) + 30,660 funding**
  적재. 단위테스트 +6, 전체 373 passed. **walk-forward 데이터 제약 해소(수개월 대기 불필요).**
- 🟥 **★ 결정적 walk-forward 발견(2년·3,400+거래)**: **1h 돌파는 NET NEGATIVE**(PF 0.95,
  expectancy_R −0.036, 누적 −305%) — P1의 "20일 +0.11"은 **노이즈였음(소표본 과적합)**.
  **타임프레임을 올리면 엣지 출현**: 4h PF 1.04(OOS 1.00, 손익분기), **1d(일봉) PF 1.39
  (OOS 1.63), 승률 54~57%, expectancy_R +0.23(OOS +0.42)** — OOS-견고한 진짜 추세추종 엣지.
  **단 1d 표본 부족**(2년 89건/OOS 30건 < 200). 기준선 `backtest_runs`
  (`breakout_walkforward_2y_timeframes_20260521`). **→ walk-forward가 1h 손실전략의 실거래를 차단.**
- ✅ **A1 신뢰 하네스 완료 + 정직 재검증** — `backtesting/portfolio_backtest.py`: 지표가
  `strategy/breakout.py`와 **수학적 동일함을 테스트로 입증**(코드 괴리 #1 해소) + **테이커 진입**
  (post-only 낙관 #2 제거) + 티어/스톱 슬리피지 + 펀딩 + **포트폴리오 동시보유·상관반영 MDD**
  (#3) + BTC 벤치마크. 단위테스트 +8(등가성 포함), 전체 381 passed. **2년 재검증 결과:
  일봉 돌파 PF 1.29·승률 53%·expR +0.146·수익 +11%·MDD 8.9%(Sharpe 0.45) vs BTC홀드
  +10%/MDD 50%** → **수익은 비슷, 낙폭은 ~1/5(저낙폭 위험조정 엣지). 1h는 사망(PF 0.92).**
  기준선 `backtest_runs`(`breakout_realistic_portfolio_2y_20260521`). **단 64거래/단일2년 = 고무적이나 미확정.**
- ✅ **A2-① 유니버스 정합 + 폭 스윕 완료** — `backtesting/universe.py`(24h 거래대금 기반
  유동성 리졸버 + 볼륨 티어, 단위테스트 6). **봇 정합 유니버스(CMC 시총≤50 ∩ $100M+ ∩ 비보호)
  = 오늘 기준 16종목**(원래 12 중 8 겹침, 중형 8 신규, TRX/DOT/LTC/APT 탈락). 18종목 2년 backfill.
  **폭 스윕(일봉 돌파)**: top4 PF1.21/Sharpe0.28 → **top8 PF2.20/Sharpe1.23**(스위트스팟) →
  top16(+중형) PF1.53/MDD13%(악화). **→ "너무 좁음"은 기회비용 실재(top4→8 개선), "중형까지
  확대"는 역효과.** 기준선 `backtest_runs`(`universe_breadth_sweep_daily_20260522`). 전체 387 passed.
- 🟥 **단, 생존편향 미교정**: top8/16에 신규 상장(HYPE~1y·ASTER~8mo·ONDO·TAO)이 포함돼 수치
  과대평가 가능 + 이력 길이 불균등 + point-in-time 아님. **이 결과도 아직 미확정.** 또한 고볼륨
  비크립토(금·은·INTC·밈 22종)는 시총게이트로 제외 — **자산군 확대는 운영자 전략 결정 사안**.
- 🟥🟥 **A2-② 결정적 발견 — top8 엣지는 착시(ZEC 단일종목 집중)**: 종목별 손익 분해 결과
  총 +$383 중 **ZEC +$370(97%)**, 8종목 중 **4종목 손실**, ZEC 제외 시 PF≈1.0(엣지 소멸).
  신규상장(HYPE −$25) 제외해도 동일(established7 PF 2.23) → 생존편향 주범은 신규상장이 아니라
  **단일종목 집중(ZEC 프라이버시 랠리)**. 연도분할도 비일관(yr1 PF0.71/yr2 1.64). **결론: 일봉
  돌파는 일반화된 강건 엣지가 없다 — 헤드라인은 한 코인 행운.** 기준선 `backtest_runs`
  (`breakout_survivorship_decompose_20260522`). **검증 인프라가 "운빨 전략" 투입을 차단.**
- ▶️ **전략 재고 필요**: 단순 돌파(1h/4h/1d) 폴리싱은 사실상 막다른 길. 선택:
  (a) **횡단면 강건성 요구** — 엣지가 다수 종목에 분산돼야 채택(단일종목 의존 배제), 또는
  (b) **전략 패러다임 전환** — 펀딩 페이드(P3)·앙상블·"살아있는 시스템" 등 다른 가설을 동일
  하네스로 검증. 어느 쪽이든 **단일 행운 종목에 기대지 않는 엣지**가 입증돼야 실거래 GO.

> 실거래 실행/주문은 전 단계에서 **금지(HOLD)**. **현재 어떤 구성도 ≥200거래 OOS 검증을 통과하지
> 못함**(1d는 고무적이나 표본 부족) → 실거래는 아직 불가. 코드 변경은 단계별·검증 후 진행.

---

## 6. 운영자 제안 검토 — BTC 시장-베타 인덱스 + CMC 글로벌 (2026-05-21, 전문 트레이더 관점)

> 제안: ① 비트코인 캔들을 "전체 코인시장 상승/하락 지수"로 항상 저장해 추후 매매전략에 활용,
> ② CoinMarketCap API 정보 활용. 아래는 객관적·현실적 평가와 채택 결론.

### 6-1. BTC 시장-베타 인덱스 — **채택(가치 높음)**
- **근거(사실)**: 알트 무기한은 BTC 와 강하게 동조한다(추세장 상관 0.7~0.9, **급락장에서 1로 수렴**).
  BTC 방향은 알트 베타의 1차 결정요인 → BTC 추세를 "시장 risk-on/off" 필터로 쓰는 것은 실무 표준.
- **현실 점검(이미 일부 존재)**: 본 봇은 이미 BTCUSDT 를 레짐 기준(`_CONTEXT_SYMBOL`, RegimeDetector)
  으로 사용한다. **신규 가치**는 (a) BTC OHLCV 의 **장기 영속 저장**(백테스트 시장-필터 피처),
  (b) 진입에 **명시적 BTC risk-off 게이트** 추가(현재 레짐엔진은 BTC를 쓰지만 알트 진입의 직접
  게이트는 아님).
- **보호종목 충돌 없음**: BTC/ETH **데이터 수집·분석은 read-only**(허용). 거래(진입/청산)는 계속
  금지 — 인덱스로만 사용하므로 룰 위반 아님.
- **현실적 한계/주의**:
  - 빈도↓: 이미 거래가 드문데 BTC 게이트가 더 줄인다 → **risk-off(하락 차단) 용도로만** 쓰고
    "BTC 상승 필수" 같은 강제조건은 지양(알트시즌·BTC 횡보-알트 펌프 구간 누락 방지).
  - 중복 회피: 레짐엔진과 이중 카운트하지 않도록 게이트를 단일 지점(진입 직전)에 둔다.
  - **반드시 백테스트 입증**: 게이트가 expectancy↑·MDD↓ 를 실제로 개선할 때만 채택(직관 추가 금지).
- **결론**: P5a 로 BTC(+ETH) OHLCV 상시 적재(즉시), P5b 로 BTC risk-off 게이트(백테스트 후 채택).

### 6-2. CoinMarketCap API — **부분 채택(글로벌 지표만, 우선순위 낮음)**
- **현실 점검(이미 존재)**: CMC 클라이언트(`data/cmc_client.py`)는 이미 PairWhitelist Tier3 **시총 순위**
  검증에 사용 중(`CMC_API_KEY` 옵션). 즉 "CMC 활용"의 상당부는 **이미 반영됨**.
- **추가 가능 가치(바이낸스가 못 주는 것만)**: **BTC 도미넌스·총 시총·Fear&Greed** 등 거래소 단일
  데이터에 없는 **시장 전역 집계**. 도미넌스↑=자금 BTC 회귀(알트 약세), ↓=알트시즌 → 보조 risk
  컨텍스트.
- **현실적 한계/주의**:
  - **가격/OHLCV 용도로 쓰지 말 것** — 체결가능한 실데이터는 바이낸스가 우월. CMC 는 **집계 지표 전용**.
  - **레이트리밋·지연**: CMC 무료티어 호출수 제한 + 분단위 지연 → 실행 신호 아님, **느린 컨텍스트**
    (시간/일 단위)로만. 1주기 1콜로 `market_global` 적재.
  - **엣지 마진 작음**: 도미넌스·F&G 는 파생·후행 지표 → 단독 엣지 약함. **risk 컨텍스트 보조**로만,
    역시 백테스트 입증 전제.
- **결론**: P5a 로 글로벌 지표(도미넌스·총시총·F&G) 적재만 추가, 전략 반영은 백테스트로 검증되면 한정 채택.

### 6-3. 종합 판정
| 제안 | 판정 | 이유 |
|---|---|---|
| BTC 시장-베타 인덱스(저장+게이트) | 🟢 **채택** | 강한 동조성, 표준 risk-off 필터, read-only로 룰 무충돌. 단 risk-off 한정·백테스트 입증 |
| CMC 글로벌 지표(도미넌스/총시총/F&G) | 🟠 **부분 채택** | 바이낸스가 못 주는 집계만 가치. 후행·레이트리밋 → 보조 컨텍스트, 우선순위 낮음 |
| CMC를 가격/OHLCV로 사용 | 🔴 **불채택** | 체결가능 실데이터는 바이낸스 우월 |
| 시총 순위(기존 CMC) | ✅ 이미 사용 중 | PairWhitelist Tier3 |

> 두 제안 모두 **데이터부터 쌓고(P5a, 즉시) → 게이트는 백테스트로 검증 후 채택(P5b)** 의 순서로,
> 기존 원칙(검증 안 된 피처를 라이브에 추가 금지, HOLD 유지)을 지킨다.
